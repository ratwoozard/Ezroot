# Web – kopier til .env.local
# API base URL (uden trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:3001
# Map style (MapLibre). Fallback = OpenFreeMap bright. Alternativer: https://tiles.openfreemap.org/styles/dark, liberty, positron.
NEXT_PUBLIC_MAP_STYLE_URL=
